# macOS
.DS_Store

## Xcode — user-specific
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

## Build output
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

## XcodeGen — the project is generated from project.yml; run `make generate`.
*.xcodeproj

## Swift Package Manager
.build/
.swiftpm/

## Dependency managers (unused, kept for safety)
Pods/
Carthage/Build/

## WhisperKit / CoreML model cache (downloaded at runtime, large)
*.mlmodelc

## Private voice recordings — never commit (minor's data)
Samples/
